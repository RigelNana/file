# 变量与输出

---

## 1. Input Variable 的定义与使用？

**回答：**

```hcl
# variables.tf

# 基本定义
variable "instance_type" {
  description = "EC2 实例类型"
  type        = string
  default     = "t3.micro"
}

# 无默认值 (必须提供)
variable "environment" {
  description = "部署环境"
  type        = string
}

# 带验证规则
variable "instance_count" {
  description = "实例数量"
  type        = number
  default     = 1

  validation {
    condition     = var.instance_count > 0 && var.instance_count <= 10
    error_message = "实例数量必须在 1-10 之间"
  }
}

# 使用变量
resource "aws_instance" "web" {
  count         = var.instance_count
  instance_type = var.instance_type
  ami           = var.ami_id

  tags = {
    Environment = var.environment
    Name        = "web-${count.index + 1}"
  }
}
```

---

## 2. 变量类型 (Type Constraints)？

**回答：**

```hcl
# 基本类型
variable "name" {
  type = string      # 字符串
}
variable "count" {
  type = number      # 数字
}
variable "enabled" {
  type = bool        # 布尔
}

# 集合类型
variable "availability_zones" {
  type = list(string)         # 有序列表
  default = ["us-east-1a", "us-east-1b"]
}

variable "instance_tags" {
  type = map(string)          # 键值对
  default = {
    Team   = "devops"
    Owner  = "admin"
  }
}

variable "unique_ids" {
  type = set(string)          # 无序不重复集合
}

# 结构化类型
variable "server_config" {
  type = object({
    instance_type = string
    disk_size     = number
    encrypted     = bool
    tags          = map(string)
  })
  default = {
    instance_type = "t3.micro"
    disk_size     = 20
    encrypted     = true
    tags          = {}
  }
}

# 元组 (固定长度, 不同类型)
variable "rule" {
  type = tuple([string, number, bool])
  # ["allow", 443, true]
}

# 嵌套复杂类型
variable "subnets" {
  type = list(object({
    cidr_block        = string
    availability_zone = string
    public            = bool
  }))
}

# any 类型 (不推荐)
variable "flexible" {
  type = any
}

# optional() 属性 (Terraform 1.3+)
variable "config" {
  type = object({
    name     = string
    port     = optional(number, 8080)      # 可选, 默认 8080
    protocol = optional(string, "tcp")
  })
}
```

---

## 3. 变量赋值的方式与优先级？

**回答：**

```
赋值方式 (优先级从低到高):

  1. default 值        → variable 块中定义
  2. terraform.tfvars  → 自动加载
  3. *.auto.tfvars     → 按字母排序自动加载
  4. -var-file         → 命令行指定文件
  5. -var              → 命令行直接指定
  6. TF_VAR_xxx        → 环境变量
  
  高优先级覆盖低优先级
```

```bash
# 方式 1: default (最低)
variable "region" {
  default = "us-east-1"
}

# 方式 2: terraform.tfvars (自动加载)
# terraform.tfvars
region         = "us-west-2"
instance_type  = "t3.small"
environment    = "production"

# 方式 3: *.auto.tfvars (自动加载)
# production.auto.tfvars
environment = "production"

# 方式 4: -var-file
terraform apply -var-file="staging.tfvars"

# 方式 5: -var (命令行)
terraform apply -var="environment=staging"

# 方式 6: 环境变量 (最高)
export TF_VAR_environment="development"
terraform apply
```

```
多环境管理常用模式:

  environments/
  ├── dev.tfvars
  ├── staging.tfvars
  └── production.tfvars

  terraform apply -var-file="environments/dev.tfvars"
```

---

## 4. 敏感变量 (Sensitive)？

**回答：**

```hcl
# 标记为敏感
variable "db_password" {
  description = "数据库密码"
  type        = string
  sensitive   = true     # 不会在 plan/apply 输出中显示
}

# 使用
resource "aws_db_instance" "main" {
  password = var.db_password
  # plan 输出: password = (sensitive value)
}
```

```
敏感变量注意:
  ✓ plan/apply 输出隐藏
  ✗ State 文件中仍以明文存储！
  ✗ 日志中可能泄露

最佳实践:
  1. 使用 Vault / AWS Secrets Manager 管理密钥
  2. 环境变量传递: TF_VAR_db_password
  3. 加密 State 文件 (SSE-KMS)
  4. 不要写入 .tfvars 文件提交 Git
```

```hcl
# 从 AWS Secrets Manager 读取
data "aws_secretsmanager_secret_version" "db_password" {
  secret_id = "production/db/password"
}

resource "aws_db_instance" "main" {
  password = data.aws_secretsmanager_secret_version.db_password.secret_string
}

# 从 HashiCorp Vault 读取
data "vault_generic_secret" "db" {
  path = "secret/production/db"
}

resource "aws_db_instance" "main" {
  password = data.vault_generic_secret.db.data["password"]
}
```

---

## 5. Output 输出值？

**回答：**

```hcl
# outputs.tf

# 基本输出
output "instance_id" {
  description = "EC2 实例 ID"
  value       = aws_instance.web.id
}

output "public_ip" {
  description = "公网 IP"
  value       = aws_instance.web.public_ip
}

# 敏感输出
output "db_connection_string" {
  description = "数据库连接串"
  value       = "postgresql://${aws_db_instance.main.endpoint}/${aws_db_instance.main.db_name}"
  sensitive   = true
}

# 条件输出
output "lb_dns_name" {
  description = "负载均衡器域名"
  value       = var.create_lb ? aws_lb.main[0].dns_name : null
}

# 复杂输出
output "instance_info" {
  value = {
    id         = aws_instance.web.id
    public_ip  = aws_instance.web.public_ip
    private_ip = aws_instance.web.private_ip
    az         = aws_instance.web.availability_zone
  }
}

# 列表输出
output "subnet_ids" {
  value = aws_subnet.public[*].id
}
```

```bash
# 查看输出
terraform output
terraform output instance_id
terraform output -json             # JSON 格式
terraform output -raw public_ip    # 纯文本 (脚本使用)

# 在脚本中使用
ssh ec2-user@$(terraform output -raw public_ip)
```

---

## 6. Local Values 本地变量？

**回答：**

```hcl
locals {
  # 简化重复表达式
  common_tags = {
    Project     = var.project_name
    Environment = var.environment
    ManagedBy   = "terraform"
    Team        = var.team
  }

  # 计算值
  name_prefix = "${var.project_name}-${var.environment}"

  # 条件逻辑
  is_production = var.environment == "production"
  instance_type = local.is_production ? "m5.xlarge" : "t3.micro"

  # 数据转换
  subnet_map = { for s in var.subnets : s.name => s.cidr_block }
}

# 使用
resource "aws_instance" "web" {
  instance_type = local.instance_type

  tags = merge(local.common_tags, {
    Name = "${local.name_prefix}-web"
  })
}

resource "aws_s3_bucket" "data" {
  bucket = "${local.name_prefix}-data"

  tags = local.common_tags
}
```

```
variable vs local:
  variable  → 外部传入的参数 (可覆盖)
  local     → 内部计算的中间值 (不可外部设置)
  
  用 local:
    ✓ 避免重复表达式
    ✓ 封装计算逻辑
    ✓ 提高可读性
```

---

## 7. Variable Validation 变量验证？

**回答：**

```hcl
# 字符串格式验证
variable "environment" {
  type = string
  validation {
    condition     = contains(["dev", "staging", "production"], var.environment)
    error_message = "环境必须是 dev, staging 或 production"
  }
}

# 正则验证
variable "bucket_name" {
  type = string
  validation {
    condition     = can(regex("^[a-z0-9][a-z0-9.-]{1,61}[a-z0-9]$", var.bucket_name))
    error_message = "S3 Bucket 名称不合法"
  }
}

# 数值范围
variable "port" {
  type = number
  validation {
    condition     = var.port >= 1 && var.port <= 65535
    error_message = "端口号必须在 1-65535 之间"
  }
}

# CIDR 格式
variable "vpc_cidr" {
  type = string
  validation {
    condition     = can(cidrhost(var.vpc_cidr, 0))
    error_message = "必须是合法的 CIDR 格式, 如 10.0.0.0/16"
  }
}

# 列表非空
variable "subnet_cidrs" {
  type = list(string)
  validation {
    condition     = length(var.subnet_cidrs) > 0
    error_message = "至少需要一个子网"
  }
}

# 多条验证 (Terraform 1.9+)
variable "instance_type" {
  type = string
  validation {
    condition     = startswith(var.instance_type, "t3.")
    error_message = "必须使用 t3 系列实例"
  }
  validation {
    condition     = !endswith(var.instance_type, "nano")
    error_message = "不允许使用 nano 实例"
  }
}
```

---

## 8. 类型转换与函数？

**回答：**

```hcl
# 自动转换
variable "enable_flag" {
  type = bool
}
# "true" (string) → true (bool) 自动转换

# 显式转换函数
locals {
  str_to_num   = tonumber("42")          # 42
  num_to_str   = tostring(42)            # "42"
  to_bool      = tobool("true")          # true
  to_list      = tolist(toset(["a","b"]))# set → list
  to_set       = toset(["a","a","b"])    # ["a","b"] 去重
  to_map       = tomap({a = 1, b = 2})   # 显式 map
}

# 常用集合操作
locals {
  # 合并 Map
  merged_tags = merge(
    var.default_tags,
    var.extra_tags,
    { Name = "my-resource" }
  )

  # 列表过滤
  public_subnets = [for s in var.subnets : s if s.public]

  # 列表 → Map
  subnet_by_az = { for s in var.subnets : s.az => s.id }

  # 扁平化嵌套列表
  all_rules = flatten([
    for sg in var.security_groups : [
      for rule in sg.rules : {
        sg_name = sg.name
        port    = rule.port
      }
    ]
  ])

  # 查找
  web_subnet = [for s in var.subnets : s if s.name == "web"][0]

  # 排序
  sorted = sort(var.zones)

  # 去重
  unique = distinct(var.items)

  # 长度
  count = length(var.items)
}
```

---

## 9. tfvars 文件组织模式？

**回答：**

```
模式 1: 按环境分文件

  project/
  ├── main.tf
  ├── variables.tf
  ├── terraform.tfvars           # 所有环境通用
  ├── environments/
  │   ├── dev.tfvars
  │   ├── staging.tfvars
  │   └── production.tfvars
  └── secrets/                   # .gitignore 忽略
      └── production.secret.tfvars

  terraform apply -var-file="environments/dev.tfvars"

模式 2: auto.tfvars 自动加载

  project/
  ├── terraform.tfvars           # 基础配置
  ├── network.auto.tfvars        # 网络配置
  ├── compute.auto.tfvars        # 计算配置
  └── tags.auto.tfvars           # 标签配置

  # 所有 *.auto.tfvars 自动加载, 按字母排序

模式 3: Workspace + tfvars

  terraform workspace select dev
  terraform apply -var-file="${terraform.workspace}.tfvars"
```

```hcl
# dev.tfvars
environment    = "dev"
instance_type  = "t3.micro"
instance_count = 1
enable_monitoring = false
db_instance_class = "db.t3.micro"

# production.tfvars
environment    = "production"
instance_type  = "m5.xlarge"
instance_count = 3
enable_monitoring = true
db_instance_class = "db.r5.large"
```

---

## 10. 变量与输出的面试常见问题？

**回答：**

```
Q: variable 和 local 的区别？
A: variable 是外部输入 (参数), 可通过 tfvars/命令行赋值
   local 是内部计算的中间值, 只能在模块内使用

Q: 如何管理敏感变量？
A: 1. sensitive = true 标记
   2. 环境变量 TF_VAR_xxx 传递
   3. 外部密钥管理 (Vault / Secrets Manager)
   4. 加密 State (SSE-KMS)
   5. 不要写入 Git

Q: 变量优先级？
A: 环境变量 > -var > -var-file > *.auto.tfvars > terraform.tfvars > default

Q: output 的用途？
A: 1. 显示部署结果 (IP, URL)
   2. 模块间传递数据
   3. 跨 State 引用 (terraform_remote_state)
   4. 脚本集成 (terraform output -raw)

Q: 如何强制用户必须提供变量？
A: 不设置 default 值
   variable "name" { type = string }
   执行时若未提供, Terraform 会交互提示或报错

Q: nullable 参数的作用？
A: Terraform 1.1+ 支持, 控制变量是否接受 null
   variable "name" {
     type     = string
     nullable = false    # 不允许 null (默认 true)
   }

Q: 如何在模块中使用变量？
A: 模块调用时传参:
   module "vpc" {
     source    = "./modules/vpc"
     vpc_cidr  = var.vpc_cidr      # 传入
   }
   模块输出:
   output "vpc_id" { value = module.vpc.vpc_id }
```
